# sales class

class Sales:

	def __init__(self, real_estate_id, account, location_address, dist, b_type, built, size, story_hgt, price, last_sold):
		self.__real_estate_id = real_estate_id
		self.__account = account
		self.__location_address = location_address
		self.__dist = dist
		self.__b_type = b_type
		self.__built = built
		self.__size = size
		self.__story_hgt = story_hgt
		self.__price = price
		self.__last_sold = last_sold